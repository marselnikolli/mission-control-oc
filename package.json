{
  "name": "openclaw-mission-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live execution map for an OpenClaw multi-agent Gateway",
  "bin": { "mc-cli": "bin/mc-cli.js" },
  "scripts": {
    "start": "node server.js",
    "probe": "node server.js --probe",
    "test": "node --test",
    "lint": "eslint ."
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ws": "^8.18.0" },
  "devDependencies": { "eslint": "^9.14.0" }
}
